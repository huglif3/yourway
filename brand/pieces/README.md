# Campaign pieces

Source PDFs as delivered (1080 × 1080), renamed to `piece-NN.pdf`.
`preview/` holds a 720px PNG of each for quick reference.

These are print/social masters. Nothing here is web-optimised — if a piece
needs to ship on the site, export it to WebP/PNG at the required size and put
it in `/assets`, don't link the PDF.
